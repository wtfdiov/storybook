import { action } from '@storybook/addon-actions';
import Button, { typeOptions, sizeOptions } from './Button';
import { Story, Meta } from '@storybook/addon-docs/blocks';
import { withKnobs, text, boolean, number, select } from "@storybook/addon-knobs";
import { withA11y } from '@storybook/addon-a11y';

<Meta title='MDX|Button' component={Button} decorators={[ withKnobs, withA11y ]} />

<Story name="Default">
  <Button type={select("type", typeOptions, typeOptions[0])} size={select("size", sizeOptions, sizeOptions[0])}>{text("Label", "Large")}</Button>
</Story>

